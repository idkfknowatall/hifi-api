services:
  web:
    build: .
    env_file:
      - ${HIFI_API_ENV_FILE:-/etc/hifi-api-work.env}
    ports:
      - "0.0.0.0:8000:8000"
    volumes:
      - ./token.json:/app/token.json:ro
    restart: unless-stopped

networks:
  default:
    driver: bridge
